# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node (dev-only Tailwind build tooling, not used in production)
node_modules/
package-lock.json
package.json

# Environment
.env

# Logs
logs/

# Data / runtime
data/api_cache.json
data/course_assignments.json
data/users.json
data/course_assignments.backup.*.json
# SQLite is now the source of truth (see db.py); the JSON files above remain as
# one-time migration backups. The live DB + its WAL sidecars are runtime state.
data/app.db
data/app.db-wal
data/app.db-shm
